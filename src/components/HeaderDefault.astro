---

interface Props {
    title: String;
}

const { title } = Astro.props;

---

<header class="secondary-header">
    <div class="container">
      <h1>{ title }</h1>
      <slot />
    </div>
</header>